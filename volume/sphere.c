#include<stdio.h>
#include<conio.h>
int main()
{
    float radius,volume;
    printf("the radius of the sphere");
    scanf("%f",&radius);
    volume=88.0/21.0*radius*radius*radius;
    printf("the volume of sphere=%f",volume);
    getch();
    return 0;
    }